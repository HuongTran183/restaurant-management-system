spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_management?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: false

app:
  bootstrap:
    demo:
      enabled: true
